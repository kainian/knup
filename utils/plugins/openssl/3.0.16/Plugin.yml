name: openssl
version: 3.0.16
type: cellar
multiVersionEnabled: true
abstract: |
  OpenSSL is a robust, commercial-grade, full-featured Open Source Toolkit 
  for the TLS (formerly SSL), DTLS and QUIC protocols.
provisions:
  - name: make
    script: |
      (eval "$(cat "${NEXT_REPOSITORY}/utils/share/openssl/3.0.16")")
